import { Directive } from '@angular/core';
import { classes } from '<%- importAlias %>/utils';

@Directive({
	selector: '[hlmButtonGroupText],hlm-button-group-text',
})
export class HlmButtonGroupText {
	constructor() {
		classes(
			() =>
				"bg-muted flex items-center gap-2 rounded-md border px-4 text-sm font-medium shadow-xs [&_[hlmIcon]]:pointer-events-none [&_[hlmIcon]:not([class*='text-'])]:text-base",
		);
	}
}
