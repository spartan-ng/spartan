{
	"$schema": "http://json-schema.org/draft-07/schema",
	"version": 2,
	"title": "GenerateUiDocs executor",
	"description": "Generates UI API documentation from component metadata",
	"type": "object",
	"properties": {
		"outputDir": {
			"type": "string",
			"description": "Directory where the per-component ui-api JSON files will be written, relative to workspace root"
		},
		"brainDir": {
			"type": "string",
			"description": "Directory containing brain components, relative to workspace root"
		},
		"helmDir": {
			"type": "string",
			"description": "Directory containing helm components, relative to workspace root"
		}
	},
	"required": ["outputDir", "brainDir", "helmDir"]
}
