{
	"$schema": "http://json-schema.org/draft-07/schema",
	"$id": "AutoIncrementVersionGenerator",
	"title": "Auto-increment version",
	"description": "Increments the alpha pre-release number read from libs/brain/package.json (the source of truth) and applies the new version to the brain, cli, and mcp packages. Used for nightly releases.",
	"type": "object",
	"properties": {}
}
