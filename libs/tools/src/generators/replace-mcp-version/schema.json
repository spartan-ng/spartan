{
	"$schema": "http://json-schema.org/draft-07/schema",
	"$id": "ReplaceMcpVersionGenerator",
	"title": "Replace MCP version",
	"description": "Sets the version of the @spartan-ng/mcp package in libs/mcp/package.json.",
	"type": "object",
	"properties": {
		"newVersion": {
			"type": "string",
			"description": "The version to set, e.g. 0.0.1-alpha.708. Falls back to the VERSION environment variable when omitted."
		}
	}
}
