{
	"$schema": "http://json-schema.org/draft-07/schema",
	"$id": "ReplaceUiVersionGenerator",
	"title": "Replace UI version",
	"description": "Sets the version of the @spartan-ng/brain package and its @spartan-ng peer dependencies, and updates the SPARTAN_VERSION constant referenced by the CLI.",
	"type": "object",
	"properties": {
		"newVersion": {
			"type": "string",
			"description": "The version to set, e.g. 0.0.1-alpha.708. Falls back to the VERSION environment variable when omitted."
		}
	}
}
