{
	"$schema": "http://json-schema.org/draft-07/schema",
	"$id": "SetReleaseVersionGenerator",
	"title": "Set release version",
	"description": "Sets the version of the published @spartan-ng packages (brain, cli, mcp) in lockstep.",
	"type": "object",
	"properties": {
		"newVersion": {
			"type": "string",
			"description": "The version to set, e.g. 0.0.1-alpha.708. Falls back to the VERSION environment variable when omitted."
		}
	}
}
